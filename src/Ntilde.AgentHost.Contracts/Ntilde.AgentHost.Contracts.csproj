<Project Sdk="Microsoft.NET.Sdk">

  <!--
    Leaf contracts library for the agent-host control channel
    (docs/agent-host/DIRECTION.md, milestone A1). Shared by Ntilde.App
    (endpoint) and Ntilde.McpServer (client). Must have zero project
    references; enforced by Ntilde.Architecture.Tests.
  -->

  <PropertyGroup>
    <!-- Ratchet (#108): warning-free today, so lock it in. See the repo-wide default in
         Directory.Build.props, which stays false until the noisy projects are cleaned. -->
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Ntilde.McpServer.Tests" />
  </ItemGroup>

</Project>
