{
  "//": "Claude Desktop MCP config for the Ntilde MCP Dev Companion (read-only, local).",
  "//build": "Build and mirror first: scripts/run-sidecar.ps1 — it prints the exact absolute path to paste below.",
  "//paths": "Claude Desktop passes these args straight to the process: there is no shell, so %LOCALAPPDATA% or $HOME would be taken literally and the DLL lookup would fail. Use the absolute path the script prints. Replace C:/Users/YOU and /absolute/path/to/nova2 with your own.",
  "//why-sidecar": "Point at the sidecar copy, NOT src/Ntilde.McpServer/bin/. The server is long-lived, so from the repo tree it holds Ntilde.AgentHost.Contracts.dll open and every full repo build fails with MSB3027 on the McpServer copy step. See issue #211.",
  "//config": "Swap Debug for Release if you launch the sidecar with -Configuration Release.",
  "//non-windows": "Linux/macOS sidecar path: /home/YOU/.local/share/ntilde-sidecar/McpServer/Debug/net10.0/Ntilde.McpServer.dll",
  "//why-not-dotnet-run": "Use the built DLL, not 'dotnet run' — run emits build/restore output to stdout, which corrupts the MCP JSON-RPC stream.",
  "mcpServers": {
    "ntilde-dev": {
      "command": "dotnet",
      "args": [
        "C:/Users/YOU/AppData/Local/ntilde-sidecar/McpServer/Debug/net10.0/Ntilde.McpServer.dll"
      ],
      "env": {
        "NTILDE_REPO_ROOT": "/absolute/path/to/nova2"
      }
    }
  }
}
