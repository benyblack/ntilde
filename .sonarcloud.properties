# SonarCloud automatic analysis reads this file. docs/ holds design specs, plans and the
# one-off scripts that produced historical commits; none of it ships, so it is not analysed.
sonar.exclusions=docs/**
